
@{
    ViewData["Title"] = "Second";
}

<h2>桃園景點</h2>
<div class="card-columns">

</div>
@section Styles{
    <style>
/*        div {
            background: black;
            color: #ffffff;
        }*/
        h2 {
            text-align: center
        }
        h1 {
            text-align: center
        }
    </style>
    <link href="~/css/bootstrap.min.css" rel="stylesheet" />
}

<h1>Homework</h1>
<ul id="ul1"></ul>

@section Scripts
{
    <script src="~/js/travel.js"></script>
    <script>
        let htmlDatas = DATAS.map(data => {
            let { Name, Toldescribe, Add, Changetime } = data
            return (
                `
                                <div class="card mb-3" style="max-width: 540px;">
                                  <div class="row g-0">
                                    <div class="col-md-8">
                                      <div class="card-body">
                                        <h5 class="card-title">${Name}</h5>
                                        <p class="card-text">${Toldescribe}</p>
                                        <p class="card-text"><small class="text-muted">地址：${Add}</p>
                                        <p class="card-text"><small class="text-muted">最後更新日期：${Changetime}</small></p>
                                      </div>
                                    </div>
                                  </div>
                                </div>
                `
            )

        })
        document.querySelector(".card-columns").innerHTML = (htmlDatas.join(""))



                //const theUl = document.querySelector("#ul1")
                ////建立一個空物件
                //const docfrag = document.createDocumentFragment()
                //for (let i = 0; i < DATAS.length; i++) {

                //    //解構賦值 Destructuring Assignement
                //    //顯示資料 DOM
                //    const txtLi = document.createTextNode(`名稱 : ${DATAS[i].Name} 地址 : ${DATAS[i].Add}`)//建立文字節點
                //    const eleLi = document.createElement("li") //<li><li>
                //    /*const elebr = document.createElement("br")*/
                //    eleLi.appendChild(txtLi)//<li>Jack<li>
                //    docfrag.appendChild(eleLi);
                //    /*docfrag.appendChild(elebr);*/
                //}
                //theUl.appendChild(docfrag);

    </script>
    <script src = "~/js/bootstrap.js" ></script>
}


